# Application Settings
spring.application.name=yt-as-harddrive
server.servlet.context-path=/api/v1

# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/ytharddrive

# JWT secret string (used only in tests) and expiration
jwt.secret=12345longandverysecretstringwith##special$$characters123
jwt.expiration-minutes=10
